<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="purchase_request_form_view" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit" type="object" string="Submit"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve"
                            class="btn-primary" invisible="state != 'submitted'"/>
                    <button name="action_reject" type="object" string="Reject"
                            invisible="state != 'submitted'"/>
                    <button name="action_reset_to_draft" type="object" string="Reset to Draft"
                            invisible="state not in ('submitted', 'rejected')"/>
                    <button name="action_convert_to_rfq" type="object" string="Convert to RFQ"
                            class="btn-primary" invisible="state != 'approved'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved,converted"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="product_id"/>
                            <field name="description"/>
                            <field name="quantity"/>
                        </group>
                        <group>
                            <field name="purchase_order_id" readonly="1"/>
                        </group>
                    </group>
                    <group>
                        <field name="justification" placeholder="Why is this needed?"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="purchase_request_list_view" model="ir.ui.view">
        <field name="name">purchase.request.list</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'approved'"
                  decoration-danger="state == 'rejected'"
                  decoration-muted="state == 'converted'">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="description"/>
                <field name="quantity"/>
                <field name="state" widget="badge"/>
                <field name="purchase_order_id"/>
            </list>
        </field>
    </record>

    <record id="purchase_request_action" model="ir.actions.act_window">
        <field name="name">Purchase Requests</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first purchase request
            </p>
            <p>
                Employees submit requests here. Once approved, Procurement
                can convert them into an RFQ.
            </p>
        </field>
    </record>

    <menuitem id="menu_purchase_request_root"
              name="Purchase Requests"
              parent="purchase.menu_purchase_root"
              action="purchase_request_action"
              sequence="5"/>

</odoo>
